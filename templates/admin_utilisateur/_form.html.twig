<div class="col-12">
    {{ form_start(form) }}
        {% if utilisateur is defined %}
            {{ include('parts/avatar.html.twig', {user: utilisateur, size: 128}) }}
        {% endif %}
        {{ form_row(form.avatar) }}
        {{ include('parts/image-delete-button.html.twig', {input_id: 'admin_utilisateur_avatar', field_name: 'avatar', current: utilisateur.avatar|default(null)}) }}
            {{ form_widget(form) }}
        {{ include('/btns/btn-form.html.twig') }}
    {{ form_end(form) }}
</div>
