<div class="col-12">
    {{ form_start(form) }}
        <div class="row form-wrapper">

            {{ include('parts/form-publication.html.twig', {form: form}) }}

            <div class="col-4">
                {{ form_row(form.nom) }}
                {{ form_row(form.categorie) }}
                {{ form_row(form.type) }}
                {{ form_row(form.prix) }}
            </div>

            <div class="col-4">
                {{ form_row(form.numero) }}
                {{ form_row(form.motCle1) }}
                {{ form_row(form.motCle2) }}
            </div>

            <div class="col-4">
                {{ include('parts/image-field.html.twig', {
                    field: form.image, input_id: 'admin_objet_image', prefix: 'image',
                    current: objet.image|default(null), fallback: NA_169 }) }}
            </div>

            <div class="col-12 mt-4">{{ form_row(form.description,{ 'attr': {'rows': '5'} }) }}</div>

            <div class="col-12 mt-3">{{ form_row(form.regles,{ 'attr': {'rows': '3'} }) }}</div>

            <section class="col-12 mt-5">
                <h2 class="heading-cardtitle">Arme et Armure</h2>
                <hr>
            </section>
            <div class="col-4">{{ form_row(form.taille) }}</div>
            <div class="col-4">{{ form_row(form.vd) }}</div>
            <div class="col-4">{{ form_row(form.forceArc) }}</div>
            
            <div class="col-4">{{ form_row(form.poids) }}</div>
            <div class="col-4">{{ form_row(form.ndArmure) }}</div>
            <div class="col-4">{{ form_row(form.reduction) }}</div>

            {{ include('/btns/btn-form.html.twig') }}
        </div>
    {{ form_end(form) }}
</div>
